import { lusitana } from "../fonts";

export default function RecipeCardsLoader() {
	return (
		<div
			className={`${lusitana.className} test-4xl text-center h-[50%] w-[50%] border shadow-2xl`}
		>
			Loading recipe cards...
		</div>
	);
}
